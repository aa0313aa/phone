# Search Console (punycode) — Quick Steps

1. Use the punycode form of your domain when registering: `xn--2p7b1pl7d.shop`.
2. Preferred verification methods:
   - DNS TXT (recommended): add the TXT record provided by Search Console to your DNS zone.
   - HTML tag: copy the meta tag, paste into `<head>` of `index.html` or upload `google-site-verification-template.html` to site root.
3. Submit sitemap: `https://xn--2p7b1pl7d.shop/sitemap.xml` in the Search Console > Sitemaps panel.
4. Request indexing: open the URL inspection tool for each important URL and click "Request indexing".

Notes:

- Use punycode `xn--2p7b1pl7d.shop` for Adsense/Search Console to avoid verification/crawler issues.
- DNS verification is fastest for domain properties and covers all subpaths.
